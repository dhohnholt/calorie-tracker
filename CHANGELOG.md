# Changelog

Notable changes to the calorie tracker, newest first. Dates reflect when the work was done, not version tags (this is a personal local project without formal releases).

## 2026-07-17

### Added
- **Multiple profiles**: the app now supports more than one person sharing a household's data — e.g. a spouse trying the app on the same device/server. A lightweight profile switcher (web: dropdown in the header; mobile: chips in Settings) lets you pick who's currently using the app; there's no password, just a "who's using it right now" selector. Food entries, weight entries, favorite foods, weekly meal plans, and personal settings (calorie goal, goal weight, weight unit, height) are fully isolated per profile. AI spend tracking stays shared across the whole household, since it's an account-level cost, not personal data.
- **Shared recipe library**: recipes are no longer siloed per profile. Any recipe anyone adds becomes visible to every profile in a shared library; each profile then adds the ones they want to their own "Saved recipes" and rates them independently (removing a recipe from your list doesn't delete it from the library or from anyone else's collection). The Meal Plan page's "Saved recipes" card now shows your personal collection plus a "Recipe library" section below it for browsing and adding recipes someone else in the household saved.
- Existing local data is migrated automatically and losslessly into a default profile (named after whatever was in the old single "Your name" setting, or "Me") the first time the server starts after upgrading — no manual steps required.

### Changed
- The old single "Your name" field in Settings is gone, replaced by the profile switcher — names are now managed per-profile via "+ Add profile" instead of a free-text settings field.

### Added (later same day)
- **Real accounts with username/password login**, replacing the no-password profile switcher above. Each person now signs up with a display name, username, and password from a login screen (web and mobile); self-serve signup is open to anyone with the app's URL, matching a small trusted-household use case rather than a public product. Sessions are opaque bearer tokens (not JWTs) stored server-side in a `sessions` table, hashed at rest, valid for 90 days, and revoked immediately on logout.
- The one profile that existed under the old switcher is claimed automatically by whoever signs up first — their existing food/weight/recipe/settings data carries over under their new login rather than starting empty. Every account after that starts fresh with default settings, same as before.
- Every API route except `/api/auth/signup`, `/api/auth/login`, and `/api/health` now requires a valid session; a request's account is derived entirely from its bearer token, never from a client-supplied id.

### Changed (later same day)
- The profile switcher UI (dropdown/chips for picking "who's using it") is gone, since a login already identifies who's using it. Web shows a "Log out" button in the header; mobile shows the account name plus a "Log out" button in Settings.

## 2026-07-16

### Added
- **Core app**: Node/Express + SQLite backend, React (Vite) dashboard, `.env`-configured USDA FoodData Central integration.
- **Food logging**: log via chat (Claude looks up nutrition and inserts directly) or via an in-app "Log food" search that hits USDA, lets you pick a match, set quantity in grams or ounces, meal, and date, with a live macro preview before saving.
- **Food entry editing**: inline edit (description, calories, macros, meal, date) and delete on every logged item.
- **Recent foods**: one-click re-log of previously logged items from the Log food card.
- **Dashboard**: Today's calories with a circular progress ring and animated count-up, protein/carbs/fat macro donut, daily nutrition-quality indicators (sugar % of calories, sodium, fiber, protein-vs-goal), a 7-day trend panel (avg kcal/day, days over goal, sparkline), a daily-calories bar chart with click-to-drill-down into that day's meals, and a weight chart with goal line, weekly rate-of-change, and a projected date to reach goal weight.
- **BMI**: computed from logged weight + a new height field in Settings; shown on the Weight card with a status category (Underweight/Normal/Overweight/Obese).
- **Daily protein goal**: derived from goal (target) bodyweight at 1.8g/kg (~0.8g/lb) — a standard mid-range target for preserving lean mass during a cut — surfaced as a status indicator on Today's summary.
- **Profile**: name field in Settings; header shows a time-of-day-aware personalized greeting ("Good morning, David") when set.
- **Meal Plan page** (new nav tab):
  - Persisted **favorite foods** list (add/remove chips, saved across sessions).
  - Persisted **saved recipes** — composed dishes with fixed, known nutrition (e.g. a batch-cooked soup) that generators treat as one fixed serving rather than re-estimating.
  - **AI-generated meal plan (Claude)** against a protein target (remaining-today or full-day): produces a natural, creatively composed meal plan (real dish names, not just ingredient lists), can pull in a modest amount of complementary ingredients not on your favorites list when it improves a dish (flagged with a **NEW** badge), and honors free-text guidance (e.g. "soup for dinner," "a crockpot meal," "no eggs").
  - **Recipe detail view**: click a saved recipe's name to open a modal with an embedded, playable source video (when a video URL is saved), the full ingredient list, and step-by-step instructions. The "+ Add recipe" form gained optional Video URL, Ingredients, and Instructions fields to support this for future recipes.
  - **Recipe ratings**: rate any saved recipe 1-5 stars (click a filled star again to clear it); the saved recipes list is sorted by rating so favorites rise to the top.
  - Added 3 more saved recipes from a second video (breakfast egg scramble w/ Canadian bacon & lentils, a high-protein latte, and baked tilapia with zucchini & black beans), and enriched the existing chicken/bean/spinach soup recipe with its full ingredient list, instructions, and source video link.
  - **Add to plan**: pick a day and meal type (Breakfast/Lunch/Dinner/Snack), then add an item from three sources — a saved recipe, a favorite food (name pulled from your list, with amount/calories/protein entered manually since favorites don't carry fixed macros), or a one-off custom meal (name + amount + macros, not saved anywhere else). No need to go through the AI generator. Each day in "This week's plan" groups its items under meal-type headers (AI-generated plans are grouped too, using the slot Claude assigns each meal) and supports removing a single item without clearing the whole day.
  - Each day's total protein/calories in "This week's plan" is now shown as a bold, prominent line at the top of the day card instead of small muted text, so the day's full nutritional impact is visible at a glance.
  - The "Favorite food" add-to-plan field is now a type-to-search box (backed by a datalist) instead of a plain dropdown — typing any substring (e.g. "me") matches anywhere in a food's name (e.g. "Equate **Me**al Replacement Shake"), not just the start.

### Fixed
- The shopping list only listed each planned meal by name (e.g. "Cheesy Beef Dipping Burrito — 1 serving") instead of what you'd actually need to buy. It now expands any plan item that matches a saved recipe into that recipe's full ingredient list; items with no saved ingredients (favorite foods, custom meals) still show as a simple name + amount line under "Other items".
- Ingredients shared by multiple recipes (e.g. Greek yogurt, part-skim mozzarella, Frank's Red Hot, chicken breast) were listed separately under each recipe instead of consolidated into one line — buying the same thing 3-4 times over. The shopping list now matches ingredient text across all of a week's recipes (accounting for minor wording differences like "Laughing Cow cheese wedges" vs "Laughing Cow Light cheese wedges") and groups them under one checkable line, with each recipe's specific amount listed underneath so you can still see how much each one needs. This text-matching is a heuristic, not true NLP — ingredients buried inside a longer unsplit phrase (e.g. a "Slaw: Greek yogurt, mayo, vinegar…" line) won't be pulled out and matched individually, so an occasional near-duplicate may still show up as its own line.
- **"Today's calories" would go blank / show 0 with all food seemingly gone after ~6pm local time (Mountain time).** Every "what is today's date" computation (`todayISO()`, the daily-calories chart range, weight-trend projections) built its ISO date string via `Date.prototype.toISOString()`, which always converts to UTC first — so once local time crossed into the next UTC calendar day (6pm MDT = midnight UTC), the app started asking for "tomorrow's" entries, found none, and looked like today's log had been wiped. No data was ever lost; it was a date-computation bug, not deletion. Fixed by deriving all "today"/date-range logic from local date components instead of UTC, and by parsing stored "YYYY-MM-DD" strings back into `Date` objects the same local-safe way everywhere (a mixed UTC-anchored `new Date("YYYY-MM-DD")` parse in the calendar-range helper would otherwise have silently shifted the whole chart back by a day).

### Added
- **Manual food entry**: the "Log food" card now has a Search / Manual entry toggle. Manual entry takes a title, an optional free-text description, a calorie count, and an optional protein amount — no USDA lookup needed, for things like restaurant meals or homemade dishes that aren't in the database. The description shows as a secondary line under the title in the log and is editable there too (added a `notes` column to food entries for this).
- **AI spend tracking**: the Anthropic API has no endpoint for checking account credit balance, so instead the AI meal-plan generator now tracks its own token usage. Every `/ai` call records `usage.input_tokens`/`output_tokens` from the Anthropic response and accumulates an estimated cost (Haiku 4.5 pricing: $1/$5 per million input/output tokens) in `settings`; "Generate meal plan (Claude)" shows a running "Estimated AI spend" total plus the cost of the last generation. It's an estimate of consumption, not your real account balance.
- Settings now has a "Check AI usage & billing (Anthropic Console) ↗" link that opens the real Anthropic Console billing page in a new tab, for the actual balance.
  - **Weekly plan**: save any generated plan to a day of the week (Mon–Sun); persisted grid shows all seven days with per-day totals and items.
  - **Shopping list**: aggregates ingredients across every saved day into one checklist, summing matching gram quantities and combining other units, with checkboxes for shopping.
  - **Categorized, collapsible favorite foods**: each favorite is tagged with a category (Protein, Dairy & Eggs, Produce, Grains & Bread, Condiments & Sauces, Snacks & Other) chosen when adding it; the chip list groups by category and collapses behind a "Show list (N)" toggle so it's out of the way when you're not actively adding to it.
- **Polish**: toast confirmations on add/edit/delete/settings actions, card hover/press micro-interactions, a custom favicon, a loading skeleton on initial load.

### Fixed
- USDA "Energy" nutrient was occasionally read in kJ instead of kcal (values ~4x too high) — now filtered to the kcal entry specifically.
- A USDA search `dataType` filter combination (`Survey (FNDDS)` alongside other types) silently 400'd, breaking some lookups.
- Food-matching for meal-plan generation could pick a wildly wrong branded product (e.g. a candy product literally named "EGGS" outranking real eggs) — now prefers standardized Foundation/SR Legacy entries, with a median-by-calories fallback among branded results to resist outliers.
- Chart tooltips could throw on hover (`iso.split is not a function`) when a formatter received a non-date value — date formatters are now defensive.
- The AI meal-plan endpoint could return truncated/invalid JSON once prompts grew richer (dish names, ingredient flags, free-text guidance) — response token limit raised from 1024 to 4096, plus a clearer error message if it ever happens again.
- The rule-based meal-plan generator failed outright (502) whenever a favorite food's name contained parentheses (e.g. "Marketside Fresh Spinach... (Fresh)", "(Hot) Freshness Guaranteed..."): USDA's search API 400s on parentheses in the query regardless of URL-encoding. Queries are now sanitized (parentheses stripped) before being sent to USDA.
- The rule-based generator's fill algorithm greedily maxed out whichever 1-2 favorite foods had the highest protein density (e.g. 300g of cheese + 225g of a protein spread and nothing else) instead of spreading across the list — unrealistic as an actual day of eating. It now round-robins across every matched food (each capped at 150g instead of 300g) and buckets the result into Breakfast/Lunch/Dinner/Snack slots for a day-shaped plan instead of one flat list.

### Removed
- The rule-based (USDA-matching) meal-plan generator — even after the round-robin/meal-slot fix it still produced mechanical, unrealistic combinations compared to the AI generator. The Meal Plan page now offers a single "Generate meal plan (Claude)" option.

### Added
- **`shared/` package**: framework-independent JS pulled out so the web app, server, and new mobile app all use the same logic instead of copies drifting apart — local-date helpers (`todayISO`, `parseISODate`, `eachDateInRange`, etc.), body-metric calculations (BMI, protein goal), nutrition-quality thresholds, weight-trend projection, USDA gram/ounce scaling, and request validation. `client/src/dates.js`, `bodyMetrics.js`, `nutritionQuality.js`, and `weightTrend.js` are now one-line re-exports of the shared versions; `QuickAddFood.jsx` imports its quantity-scaling helpers from there instead of defining them locally. Covered by 74 `node --test` unit tests in `shared/test/`.
- **Server-side request validation**: `shared/validation.js` adds reusable validators for ISO calendar dates (rejecting overflow like Feb 30, not just the YYYY-MM-DD shape), meal types, weight units, positive/nonnegative finite numbers, and height/calorie-goal/weight-goal ranges. `POST`/`PUT /api/food-entries`, `POST /api/weight-entries`, and `PUT /api/settings` all now return a clear `400` with a human-readable message on invalid input instead of either silently accepting bad data or crashing.
- **Mobile app foundation** (`mobile/`): a new Expo/React Native app, the first milestone toward an iOS companion to the web dashboard. Expo Router with a bottom-tab layout (Today, Log, Progress, Plan, Settings), a theme module mirroring the web app's light/dark CSS custom properties, safe-area handling, and a small `fetch`-based API wrapper pointed at `EXPO_PUBLIC_API_URL` (defaults to `http://localhost:3001/api`; never bundles a USDA or Anthropic key — all such calls stay server-side). Today shows calories/goal/remaining/protein with pull-to-refresh; Log supports USDA search, gram/ounce quantity, meal + date selection, a macro preview, saving, and re-logging recent foods; Progress shows the latest weight vs. goal and a log/replace-today's-weight form. Plan and Settings are placeholders for now. `shared/` code is consumed via a local `"calorie-tracker-shared": "file:../shared"` npm dependency (an actual symlinked package, not a relative path) — Metro (React Native's bundler), unlike Vite, could not reliably resolve deep relative imports that cross outside `mobile/`'s project root even with `watchFolders` configured, so importing it as a proper local package sidesteps that.
- **Server test suite** (`server/test/`, `node --test`): route-level integration tests against a real Express app + throwaway SQLite database (via a new `DB_PATH` env override in `server/src/db.js`, defaulting to the existing on-disk database so normal behavior is unchanged) covering food-entry, weight-entry, and settings validation, plus the weight-entries upsert-by-date behavior. `server/src/index.js` now exports the Express `app` and only calls `.listen()` when run directly, so tests can drive it without binding the real port.

### Fixed
- New database initialization no longer seeds a synthetic 235 lb weight entry — that was placeholder data for early development, not something a fresh install should invent. Existing weight history is untouched; this only changes what happens on a brand-new, empty database.
- `todayISO()`/`eachDateInRange()` were already fixed earlier for the UTC-evening bug (see above), but the fix now lives once in `shared/dates.js` instead of being duplicated; a Mountain-Time-evening regression test lives in `shared/test/dates.test.js` to keep it fixed.
- `QuickAddFood.jsx`'s gram/ounce toggle referenced `OZ_TO_G` without importing it (a leftover from moving the quantity-scaling helpers to `shared/nutritionScaling.js` earlier this session) — switching units would have thrown a `ReferenceError`. Now imported alongside the other scaling helpers.

### Added (mobile)
- **Mobile Today screen now shows logged food**, grouped by meal (Breakfast/Lunch/Dinner/Snack) with per-meal subtotals, each entry's calories, and a delete button (with a confirmation prompt) — mirrors the web dashboard's meal breakdown. Added `deleteFoodEntry` to `mobile/src/api.js`.
- **Prior-day browsing on mobile Today**: `‹`/`›` date navigation (capped at today) lets you step backward through previously logged days; the header switches between a personalized greeting (today only), "Yesterday", or a short formatted date, and the calorie/protein/food-logged sections all reload for the selected date.
- **iOS bundle identifier** (`com.dhohnholt.calorietracker`) set in `mobile/app.json`, required for signing local Xcode builds or TestFlight submissions against a real Apple Developer account.
- **Real app icon**: `mobile/assets/icon.png` was still Expo's default placeholder, which is why TestFlight showed no icon. Replaced with a flattened, alpha-free 1024×1024 render of the same flame glyph used for the macOS dock app icon (from `client/public/favicon.svg`, corners squared off since iOS applies its own rounding mask — a pre-rounded or transparent source is what App Store Connect rejects). Also dropped directly into the already-generated `mobile/ios/.../AppIcon.appiconset/` so the existing Xcode project (with its Team ID and signing already configured) doesn't need `expo prebuild` re-run.
- **Export compliance declaration**: `ITSAppUsesNonExemptEncryption: false` set in both `mobile/app.json` (`ios.infoPlist`) and directly in the generated `mobile/ios/CalorieTracker/Info.plist`, since the app only talks to its own server over standard iOS networking with no custom or proprietary cryptography — this suppresses the "App Encryption Documentation" prompt App Store Connect otherwise shows on every upload.

### Fixed (mobile)
- The generated `mobile/ios/CalorieTracker/Info.plist` hardcoded `CFBundleVersion` as the literal string `"1"` instead of referencing the `$(CURRENT_PROJECT_VERSION)` Xcode build setting — bumping the build number in Xcode's project settings silently had no effect on the actual uploaded build number. Fixed to reference the variable.

### Added
- **`railway.json`**: config for deploying `server/` to Railway so the mobile app (and eventually the web client) can reach it without depending on the developer's Mac being on and on the same Wi-Fi network. Points Railway's build/start commands at the `server/` subdirectory of this monorepo and health-checks the existing `/api/health` route.

### Fixed
- **`server/src/db.js` crashed on every fresh deploy** (Railway healthcheck stuck failing "service unavailable" indefinitely): `server/data/` is gitignored — it holds the real local database file, so it's guaranteed not to exist in a fresh checkout — and `better-sqlite3` doesn't create missing parent directories itself; it throws, which crashed the process before Express ever bound to a port. This was invisible locally only because that folder already exists from months of local dev. Now creates the database's parent directory (`fs.mkdirSync(..., { recursive: true })`) before opening it, which also makes `DB_PATH` fully self-sufficient for a fresh persistent volume mount (e.g. Railway's `/data`) with no manual directory setup.

### Deployed
- `server/` is now live on Railway with a persistent volume for the SQLite file — `/api/health` and a real database read (`/api/settings`) both verified working post-deploy. `mobile/.env`'s `EXPO_PUBLIC_API_URL` now points at the Railway URL instead of the developer's LAN IP, so the mobile app works away from home Wi-Fi. README's deployment section rewritten to document the real setup instead of the earlier "you could deploy this later" note.
- **Seeded the deployed database from local data**: settings, all 8 food entries, the 1 weight entry, all 58 favorite foods, all 11 recipes, and all 3 saved weekly-plan days copied from the local SQLite database to Railway via a one-off script hitting the existing REST API (not a raw file copy), verified afterward by re-reading every endpoint and comparing counts/values against the source. `logged_at` timestamps were regenerated (not preserved) since the food/weight-entry routes always stamp server-side time on insert; the `date` field itself — what the app's charts and summaries actually use — carried over exactly. Record IDs differ between the two databases (fresh auto-increment), which doesn't affect anything since nothing depends on cross-environment ID matching.

### Fixed
- **The Railway deployment had no persistent volume attached**, despite `DB_PATH` correctly pointing at `/data/calorie-tracker.db` — so `/data` was just ordinary ephemeral container storage, silently wiped on every redeploy (including the very next `git push`, which cost the first migration entirely). Fixed by creating and attaching an actual volume mounted at `/data` in Railway's dashboard (not something expressible in this repo's config), then re-running the migration and confirming the data survives a redeploy before trusting it.
- **Web client now points at Railway instead of only `localhost:3001`**, so it and the mobile app share the same data instead of drifting apart as two independent databases. `client/vite.config.js`'s dev proxy target is now read from `VITE_API_PROXY_TARGET` (via a gitignored `client/.env`, mirroring the mobile app's `EXPO_PUBLIC_API_URL` pattern), defaulting to `http://localhost:3001` when unset so a fresh clone — or local feature development/testing — never accidentally reads or writes the real deployed data. Added `client/.env.example` documenting the override.

### Added
- **Mobile Settings screen** (`mobile/app/(tabs)/settings.js`): was a "coming soon" placeholder, now matches the web app's `SettingsModal.jsx` — profile name, daily calorie goal, goal weight, weight unit (lbs/kg toggle), and height (unit-aware, in/cm conversion via `shared/bodyMetrics.js`, same as web). Protein goal is shown as a read-only computed value derived from goal weight rather than a separate editable setting, since that's how the underlying data model actually works (`proteinGoalGrams(goalWeight, weightUnit)`, not a stored field) — same behavior as the web dashboard's indicator. Includes the same "Check AI usage & billing" link as web, opened via `Linking.openURL`.
- **Tablespoons as a food-logging unit** (web and mobile): unlike ounces, a tablespoon is a volume, and grams-per-tablespoon depends heavily on the food (16g for peanut butter, 21g for honey, 4g for granulated sugar) — a single generic conversion factor would silently mislog most foods. `shared/nutritionScaling.js`'s new `gramsPerTablespoon(food)` derives an accurate, food-specific factor from USDA's own household-serving data (`householdServingFullText` like "2 Tbsp" paired with a gram-based `servingSize`), only when that data is actually mass-based (not `mL`, which can't be converted to grams without density) and the food is on a genuine tablespoon serving (not cups, packets, etc.). `server/src/routes/nutrition.js` computes this per search result as `gramsPerTbsp`; the "tbsp" unit option is only shown in the Log UI when a selected food actually has it — foods without reliable data (most Foundation/SR Legacy entries) simply don't offer tbsp, rather than guessing. Covered by 10 new `shared/test/nutritionScaling.test.js` cases (fractional tablespoon counts, case-insensitivity, mass-vs-volume rejection, distinct per-food densities).

### Fixed
- **Mobile's g/oz unit toggle didn't actually convert the amount**, just relabeled the unit in place (switching from 100g to "oz" left the number at 100, now meaning 100oz instead of ~3.5oz) — a real mislogging bug, silent until you noticed the calorie preview looked wildly wrong. Fixed alongside adding tablespoons, since the same conversion logic now covers all three units correctly (mirrors the web app's existing `handleUnitChange` behavior, which was already correct).

### Added
- **Count-based logging units** (web and mobile) — e.g. "3 eggs" instead of converting to grams. Generalizes the tablespoon work: `shared/nutritionScaling.js`'s new `parseCountUnit(food)` derives a food's own natural counting unit (egg, slice, cookie, packet — whatever USDA's household-serving text actually says, e.g. "1 EGG") the same accuracy-first way as tablespoons — gram-based servings only, excluding anything that's actually a recognized volume/weight measure word (so it never double-counts what tbsp already handles). `unitConversionFactor(unit, food)` centralizes resolving which factor applies to the currently selected unit (tbsp or a food's own count label), used by both `QuickAddFood.jsx` and mobile's `log.js` instead of each duplicating the tbsp-specific logic. The unit button shows the food's actual word ("egg", "slice", ...), not a generic "count". 14 new test cases.
- **`Cache-Control: no-store` on `/api/nutrition/search`**: found while verifying the above — a food search response could be served stale by an intermediate cache indefinitely, since results depend on both live USDA data and our own transformation logic (which just changed twice in one session). Real fix regardless of what actually caused the specific staleness observed during testing (which turned out to be the deployed Railway server lagging behind local changes, not caching — see below).

### Added
- **Barcode scanning** (mobile only — inspired by comparing against MyFoodDiary, minus their exercise tracker which wasn't wanted): new `mobile/src/components/BarcodeScanner.js` using `expo-camera`'s `CameraView` with `barcodeScannerSettings` restricted to product barcode formats (`ean13`, `ean8`, `upc_a`, `upc_e`). No new server-side lookup was needed — USDA's existing `/api/nutrition/search` already resolves an exact UPC/GTIN correctly when searched as plain text (verified against a real barcode before building anything), so a scan just feeds the decoded digits into the exact same search flow already used for typed queries. Added a camera-icon button next to the Log screen's search bar; a full-screen scan overlay requests camera permission via `useCameraPermissions` and hands the first decoded barcode back to the existing result-selection UI.

**Known limitation from this change**: adding `expo-camera` required a full `expo prebuild` regeneration of `mobile/ios/`, which picked up the app's current display name ("DH Calorie Tracker", changed earlier this session to resolve an App Store naming collision) and renamed the Xcode project from `CalorieTracker` to `DHCalorieTracker`. **Open `mobile/ios/DHCalorieTracker.xcworkspace` from now on** — the old `CalorieTracker.xcworkspace` no longer exists. The bundle identifier (`com.dhohnholt.calorietracker`, what actually matters for App Store Connect/TestFlight continuity) is unchanged. Team ID, the `CFBundleVersion` build-number variable reference, and the app icon were manually re-verified/restored after the regeneration since those don't survive a full `prebuild` (the app icon did regenerate correctly from `assets/icon.png` on its own this time, unlike the first manual patch). Build number continues at 8.
- **Web dashboard hero banner**: `App.jsx`'s header is now a photo banner (user-supplied Middle Eastern appetizer spread, chosen for its clear center) with the time-of-day greeting overlaid in white over the image's open center area; nav tabs and the settings gear moved to a slim utility bar above it. `App.css`'s `.hero-banner`/`.hero-banner__title` hand the greeting a fixed white/text-shadow treatment (independent of light/dark theme, since the photo itself doesn't change) and shrink on narrow viewports. Verified in both themes and at mobile width.

### Fixed
- **Branded foods (real grocery products, not just generic USDA reference entries) were often crowded out of search results** by USDA's own relevance ranking — e.g. searching "yogurt" returned only 2 branded products out of 10 results, with generic SR Legacy entries taking the rest, no matter how large `pageSize` was raised (verified: raising it to 25 still only surfaced 2/25 branded, since the extra slots just went to more generic entries). Fixed `server/src/routes/nutrition.js`'s `searchUSDAFoods` to query Branded and Foundation/SR Legacy separately and merge them, guaranteeing up to 10 branded results appear in every search regardless of how USDA ranks them relative to generic entries. Verified across several searches ("yogurt", "cheerios", "chicken breast", "spinach") after the fix.

### Added
- **"Adjust" — edit calories/macros before saving a searched result** (web and mobile): found via barcode-scanning a real product where USDA's own listed nutrition data (last modified 2020) didn't match the current package — the app's calculation was correct given USDA's numbers, but USDA's numbers themselves were stale for that product, and there was previously no way to correct that without abandoning search/scan entirely for "Manual entry" (which only supports calories + protein, not carbs/fat). An "Adjust" link next to the calorie preview reveals editable Calories/Protein/Carbs/Fat fields pre-filled with the computed values; saving uses the edited numbers while fiber/sugar/sodium still come from USDA's data. "Reset to calculated" discards the edits. Verified end-to-end against the real mismatched product (Ole Mexican Foods High Fiber Carb Lean Tortilla Wraps, UPC 027331032128): USDA/app computed 50 kcal for one 45g tortilla, package says 60 — adjusted and confirmed the corrected value round-trips through save correctly.

### Fixed
- **Xcode Organizer's "Upload Symbols Failed" warnings for `React.framework`, `ReactNativeDependencies.framework`, and `ExpoCameraBarcodeScanning.framework`** (the last one new since barcode scanning was added). These frameworks were being distributed as precompiled binaries — Expo's SDK 57 speed optimization — and precompiled binaries don't ship dSYMs, so there was nothing for Xcode to upload no matter how the archive was built. Set `ios.buildReactNativeFromSource: true` and `EXPO_USE_PRECOMPILED_MODULES: false` in `mobile/ios/Podfile.properties.json`, forcing these to compile from source instead, which does generate proper dSYMs (confirmed: the `[Expo-precompiled] Precompiled modules:` list that previously named these frameworks during `pod install` is now empty). Trade-off: local `pod install`/build times increase noticeably (~48s vs ~15-20s for `pod install` alone; full Xcode builds will take meaningfully longer too) since these now compile locally instead of downloading prebuilt binaries.

**Known limitation**: `Podfile.properties.json` lives inside the gitignored `mobile/ios/` (regenerated by `expo prebuild`, not tracked), so this fix does not survive a future `expo prebuild --clean` — if that's ever run again, re-add both properties to `Podfile.properties.json` and re-run `pod install` before archiving. No `expo-build-properties`-based way to make this survive prebuild automatically was available (that plugin doesn't currently expose these two flags).
